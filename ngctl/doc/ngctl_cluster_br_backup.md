## ngctl svcgrp br backup

Backup data files to external storage for restore

### Options

```
      --agents string          Specify agents address, eg: 192.168.8.1:6688,192.168.8.2:6688,192.168.8.3:6688
      --ca-path string         Specify CA file path (default "/usr/local/certs/ca.crt")
      --cert-path string       Specify cert file path (default "/usr/local/certs/client.crt")
      --svcgrpId int          Specify the backup svcgrp id
      --concurrency int        Max concurrency for download data and data playback (default 5)
      --debug                  Output log in debug level or not
      --enable-ssl             Enable SSL connection
  -h, --help                   help for backup
      --insecure-skip-verify   Verify the server's certificate chain and host name
      --key-path string        Specify key file path (default "/usr/local/certs/client.key")
      --log string             Specify br detail log path (default "br.log")
      --meta string            Specify meta server
      --name string            Specify backup name
      --password string        Password for login metad service
      --s3.access_key string   S3 Option: set access key id
      --s3.endpoint string     S3 Option: set the S3 endpoint URL, please specify the http or https scheme explicitly
      --s3.region string       S3 Option: set region or location to upload or download backup
      --s3.secret_key string   S3 Option: set secret key for access id
      --server-name string     The subject alternative name (SAN) of the peer server to verify
      --storage string         backup target url, format: <SCHEME>://<PATH>.
                                   <SCHEME>: a string indicating which backend type. optional: local, s3.
                                   now only s3-compatible is supported.
                                   example:
                                   for local - "local:///the/local/path/to/backup"
                                   for s3  - "s3://example/url/to/the/backup"
                                   
      --username string        Username for login metad service
```

### Options inherited from parent commands

```
  -c, --svcgrp string   Cluster name
```

### SEE ALSO

* [ngctl svcgrp br](ngctl_svcgrp_br.md)	 - Backup the data of a svcgrp.
* [ngctl svcgrp br backup full](ngctl_svcgrp_br_backup_full.md)	 - Full backup data files

###### Auto generated by spf13/cobra on 26-Jul-2024
