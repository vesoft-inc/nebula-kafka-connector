name: nightly

on:
  workflow_dispatch:
    inputs:
      version:
        description: "version such as '5.1.0'"
        required: true
        default: "5.0.0"
  
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash
env:
  OSS_DIR: minio/rc-build/nebula-ng-tools
  VERSION: ${{ github.event.inputs.version }}

jobs:
  build:
    name: build source code and binary
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.work
          cache: false
      - name: setup java 
        uses: actions/setup-java@v3
        with:
          distribution: 'adopt'
          java-version: '8'
      - name: package source code
        run: |
          cd package
          ./package.sh --product=java --release-name=$VERSION --package-name=nebula-java
          ./package.sh --product=golang --release-name=$VERSION --package-name=nebula-golang
          ./package.sh --product=ngql --release-name=$VERSION --package-name=nebula-ngql
          ./package.sh --product=ngctl --release-name=$VERSION --package-name=nebula-ngctl
          ./package.sh --product=operator --release-name=$VERSION --package-name=nebula-operator
      - name: build ngctl
        run: |
          cd ngctl
          make 
      - name: build ngql
        run: |
          cd ngql
          make
      - uses: vesoft-inc/.github/actions/setup-minio@master
        with:
          minio_url: ${{ secrets.MINIO_ENDPOINT }}
          access_key: ${{ secrets.MINIO_KEY }}
          secret_key: ${{ secrets.MINIO_SECRET }}
        
      - name: upload asserts
        run: |
          mc cp ngql/ngql $OSS_DIR/$VERSION/
          mc cp ngctl/ngctl $OSS_DIR/$Version/
          mc cp -r package/packages/ $OSS_DIR/$Version/packages/

  docker:
    name: build docker image
    runs-on: self-hosted
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - uses: docker/login-action@v3
        with:
          registry: ${{ secrets.HARBOR_REGISTRY }}
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build and push Docker images - ngctl
        uses: docker/build-push-action@v3
        with:
          platforms: linux/amd64,linux/arm64
          tags: reg.vesoft-inc.com/vesoft-ng/ngctl:nightly
          push: true
          context: .
          file: ./ngctl/Dockerfile
          target: ngctl
      - name: Build and push Docker images - ngql
        uses: docker/build-push-action@v3
        with:
          platforms: linux/amd64,linux/arm64
          tags: reg.vesoft-inc.com/vesoft-ng/ngql:nightly
          push: true
          context: .
          file: ./ngql/Dockerfile
          target: ngql
